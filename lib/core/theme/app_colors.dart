import 'package:flutter/material.dart';

class AppColors {
  // 🌿 Brand
  static const primary = Color.fromARGB(255, 87, 15, 0);
  static const primaryDark = Color(0xFF2E7D32);
  static const secondary = Color(0xFF8BC34A);

  // 🌱 Backgrounds
  static const background = Color(0xFFF6F8F4);
  static const surface = Colors.white;

  // 🌿 Text
  static const textPrimary = Color(0xFF1B1B1B);
  static const textSecondary = Color(0xFF6B7280);

  // 🌱 UI
  static const border = Color(0xFFE5E7EB);
  static const divider = Color(0xFFF0F0F0);

  // 🌿 States
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFE53935);
}
